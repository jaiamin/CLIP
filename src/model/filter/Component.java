package model.filter;

/**
 * This is an enumeration representing the color components of an RGB pixel.
 * Each enum constant represents a single color component: RED, GREEN, or BLUE.
 */
public enum Component {
  RED, GREEN, BLUE
}
