package model.filter;

/**
 * This is an enumeration representing the type of brightness adjustment to be made by
 * brightness filters.
 * Each enum constant represents a single brightness adjustment: VALUE, INTENSITY, or LUMA.
 */
public enum LightType {
  VALUE, INTENSITY, LUMA
}
